services:

  news_intelligence:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: news_intelligence

    env_file:
      - .env

    networks:
      - backend

networks:
  backend:
    external: true